systems:
    middleware: { type: local-example, roundtrip: true }
    mock: { type: mock, types-from: middleware }

routes:
    mock_to_middleware: { from: mock, to: middleware }
    middleware_to_mock: { from: middleware, to: mock }

topics:
    to_middleware: { type: coordinate2d, route: mock_to_middleware, remap: { middleware: { topic: position3, type: coordinate3d } } }
    from_middleware: { type: coordinate2d, route: middleware_to_mock, remap: { middleware: { topic: position3, type: coordinate3d } } }

