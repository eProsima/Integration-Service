systems:
  ros2: { type: ros2 }
  mock: { type: mock, types-from: ros2 }

routes:
  mock_to_ros2: { from: mock, to: ros2 }
  ros2_to_mock: { from: ros2, to: mock }

topics:
  transmit: { type: "is_ros2_test_msg/BoundedArrayNested", route: ros2_to_mock }
  echo: { type: "is_ros2_test_msg/BoundedArrayNested", route: mock_to_ros2 }
