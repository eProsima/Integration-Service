systems:
    xtypes: { type: xtypes-example, roundtrip: true }
    mock: { type: mock, type-matching: true }

routes:
    mock_to_xtypes: { from: mock, to: xtypes }
    xtypes_to_mock: { from: xtypes, to: mock }

topics:
    to_xtypes: { type: coordinate2d, route: mock_to_xtypes, remap: {xtypes: { topic: position2, type: coordinate2d } } }
    from_xtypes: { type: coordinate2d, route: xtypes_to_mock, remap: {xtypes: { topic: position2, type: coordinate2d } } }

